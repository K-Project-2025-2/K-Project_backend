spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/app_db
    username: app_user
    password: app1234
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true   # SQL 콘솔 출력 (선택)
    properties:
      hibernate.format_sql: true

  mail:                           # ✅ 이메일 인증용 (추가)
    host: smtp.gmail.com          # Gmail 또는 학교 SMTP 서버 주소
    port: 587
    username: shwltjq1@gmail.com    # 발신용 이메일
    password: roriosjieopipqhq       # 앱 비밀번호(일반 비밀번호 X)
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

    data:
      redis:
        host: localhost
        port: 6379

jwt:                              # ✅ JWT 관련 (추가)
  secret: your-super-secret-key-1234567890abcd   # 32바이트 이상
  access-exp-min: 60              # 토큰 만료 시간 (분)
  issuer: KProject

app:                              # ✅ 커스텀 앱 설정 (추가)
  allowed-email-domain: kangnam.ac.kr  # 학교 메일만 허용

logging:                          # ✅ 로그 레벨 (선택)
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.springframework.security: INFO

server:
  port: 8080                      # ✅ 기존 그대로 유지
